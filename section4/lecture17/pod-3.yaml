apiVersion: v1
kind: Pod
metadata:
  name: pod-2
  namespace: nm-4
spec:
  containers:
    - name: container
      image: tmkube/app
      resources:
        requests:
          memory: 0.5Gi
        limits:
          memory: 0.5Gi
